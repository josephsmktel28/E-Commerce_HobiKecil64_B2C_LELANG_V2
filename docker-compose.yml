version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hobikecil-app
    restart: unless-stopped
    working_dir: /app
    ports:
      - "8080:8080"
    environment:
      APP_NAME: HobiKecil
      APP_ENV: local
      APP_DEBUG: "true"
      APP_URL: http://localhost:8080
      DB_CONNECTION: mysql
      DB_HOST: db
      DB_PORT: 3306
      DB_DATABASE: ${DB_DATABASE:-hobikecil}
      DB_USERNAME: ${DB_USERNAME:-hobikecil}
      DB_PASSWORD: ${DB_PASSWORD:-password}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      SESSION_DRIVER: database
      CACHE_STORE: database
      QUEUE_CONNECTION: database
    volumes:
      - ./:/app
      - /app/node_modules
      - /app/vendor
    networks:
      - hobikecil-network
    depends_on:
      - db
      - redis

  db:
    image: mysql:8.0-alpine
    container_name: hobikecil-db
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: ${DB_DATABASE:-hobikecil}
      MYSQL_USER: ${DB_USERNAME:-hobikecil}
      MYSQL_PASSWORD: ${DB_PASSWORD:-password}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-password}
    volumes:
      - hobikecil-db:/var/lib/mysql
    networks:
      - hobikecil-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: hobikecil-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - hobikecil-redis:/data
    networks:
      - hobikecil-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  hobikecil-db:
  hobikecil-redis:

networks:
  hobikecil-network:
    driver: bridge
